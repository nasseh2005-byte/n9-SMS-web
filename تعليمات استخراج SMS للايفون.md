# تعليمات استخراج SMS للآيفون

> آخر تحديث: 1 سبتمبر 2026 — توقيت الرياض  
> الغرض: استخراج رسائل SMS من iPhone بأكبر درجة اكتمال ممكنة، التحقق من النتيجة، ثم تحويلها إلى XML متوافق مع N9 SMS Web من دون تغيير التواريخ أو تكرار الرسائل.  
> مهم: هذا الدليل لا يفترض أن iMazing أو النسخة الاحتياطية أو iCloud تعطي أرشيفاً كاملاً تلقائياً. اكتمال الرسائل يجب إثباته بالتقرير والمقارنة.

---

## 1. الخلاصة التنفيذية

iPhone لا يوفر زر تصدير رسمي إلى صيغة **SMS Backup & Restore XML**. المسار العملي هو:

```text
iPhone / Messages in iCloud / نسخة Apple قديمة / Mac متزامن
                              ↓
                 قاعدة Apple الأصلية للرسائل
                    sms.db أو chat.db
                              ↓
                  تقرير اكتمال قبل التحويل
                              ↓
                 محول N9 iPhone → XML
                              ↓
                دمج آمن مع XML الموجود
```

لا تعتمد على PDF أو لقطة شاشة أو تصدير TXT وحده لبناء أرشيف أدلة؛ هذه تنسيقات عرض وقد تفقد معرفات Apple، نوع الخدمة، العلاقات والمرفقات.

---

## 2. حدود يجب معرفتها قبل البدء

### 2.1 تعطيل iCloud ليس ضماناً لتنزيل كل التاريخ

Apple تؤكد أن الرسائل عند تشغيل **Messages in iCloud** تتم مزامنتها منفصلة عن النسخة الاحتياطية اليومية، وأن الرسائل والمرفقات تُحفظ في النسخة التالية بعد إيقاف Messages in iCloud. لكنها لا تقدم ضماناً بأن مجرد إيقاف الخيار سينزّل كل سجل iCloud القديم فوراً إلى iPhone.

المرجع الرسمي:

- [إذا كانت معلومات مفقودة بعد استعادة iPhone](https://support.apple.com/en-ie/102325)
- [ما الذي يتضمنه iCloud Backup؟](https://support.apple.com/en-au/108770)

لذلك لا تستخدم القاعدة التالية:

```text
أوقف iCloud = أصبحت كل الرسائل موجودة
```

القاعدة الصحيحة:

```text
أوقف المزامنة أو استخدم Mac عند الحاجة، ثم افحص قاعدة الرسائل وعددها وتواريخها قبل الاعتماد عليها.
```

### 2.2 إعداد Keep Messages قد يكون حذف التاريخ القديم

على iPhone افتح:

```text
الإعدادات
→ التطبيقات
→ الرسائل
→ Keep Messages / الاحتفاظ بالرسائل
```

اجعله **Forever / للأبد** لمنع الحذف التلقائي مستقبلاً. إذا كان مضبوطاً على 30 يوماً أو سنة، تحذف Apple الرسائل والمرفقات بعد المدة. تغيير الإعداد الآن لا يعيد ما حُذف سابقاً.

المرجع: [حذف الرسائل والمرفقات على iPhone](https://support.apple.com/en-gb/guide/iphone/iph2c9c4bfcb/26/ios/26)

### 2.3 أي أداة لا تستطيع صناعة بيانات غير موجودة

- إذا كانت الرسالة موجودة في `sms.db` أو `chat.db` يمكن استخراجها.
- إذا كانت موجودة في نسخة قديمة فقط، يجب قراءة تلك النسخة.
- إذا كانت محذوفة من الأجهزة وiCloud وكل النسخ، فلا يستطيع المحول استعادتها.
- الرسائل في **Recently Deleted** تبقى مدة محدودة؛ راجعها قبل حذفها نهائياً.

---

## 3. مصادر الرسائل بالترتيب

لا تفترض أن مصدراً واحداً هو الكامل. احصر كل ما تملكه:

1. iPhone الحالي.
2. أي iPhone قديم لم يُمسح.
3. نسخ Apple Devices أو iTunes أو Finder القديمة.
4. Mac متزامن مع Messages in iCloud.
5. نسخة Mac احتياطية مثل Time Machine إذا كانت تحوي مجلد Messages.
6. Recently Deleted على الأجهزة المتزامنة.

أفضل نتيجة قد تأتي من دمج عدة قواعد ثم إزالة التكرار بواسطة `GUID` الأصلي للرسالة.

---

## 4. طريقة Windows: إنشاء نسخة Apple محلية

### 4.1 تجهيز iPhone

1. اشحن الجهاز ووفر مساحة تخزين كافية.
2. حدث iOS إذا كان التحديث آمناً ولا يعرّض بيانات حالية للخطر.
3. تأكد أن Keep Messages مضبوط على Forever.
4. افتح أقدم محادثات مهمة وتأكد أن نصوصها تظهر فعلياً.
5. لا تحذف Recently Deleted قبل الاستخراج.
6. لا تمسح الجهاز ولا تستعد نسخة قديمة فوقه.

### 4.2 إنشاء النسخة

1. افتح تطبيق **Apple Devices** على Windows.
2. صِل iPhone بكيبل موثوق.
3. افتح iPhone واضغط **Trust / الوثوق** عند الطلب.
4. اختر النسخ إلى هذا الكمبيوتر.
5. فعّل **Encrypt local backup**.
6. ضع كلمة مرور قوية واحفظها في مكان آمن؛ فقدانها قد يجعل النسخة غير قابلة للقراءة.
7. نفذ **Back Up Now** وانتظر حتى يظهر وقت اكتمال حديث.

Apple توضح طريقة النسخ المشفر هنا: [About encrypted backups](https://support.apple.com/en-us/108353)

### 4.3 لا تستبدل النسخة القديمة

بعد اكتمال النسخة، انسخ مجلدها إلى قرص آخر قبل تشغيل أدوات التحليل. اعمل دائماً على نسخة عمل واترك الأصل للرجوع والمقارنة.

المسار الحديث المعتاد:

```text
C:\Users\<اسم المستخدم>\Apple\MobileSync\Backup\<BACKUP_ID>
```

المسار التاريخي لـiTunes:

```text
C:\Users\<اسم المستخدم>\AppData\Roaming\Apple Computer\MobileSync\Backup\<BACKUP_ID>
```

للعثور على أحدث مجلد باستخدام PowerShell:

```powershell
$n9BackupRoots = @(
  (Join-Path $env:USERPROFILE "Apple\MobileSync\Backup"),
  (Join-Path $env:APPDATA "Apple Computer\MobileSync\Backup")
)

$n9Backups = foreach ($n9BackupRoot in $n9BackupRoots) {
  if (Test-Path -LiteralPath $n9BackupRoot) {
    Get-ChildItem -LiteralPath $n9BackupRoot -Directory
  }
}

$n9Backups |
  Sort-Object LastWriteTime -Descending |
  Select-Object FullName, LastWriteTime
```

اختر مجلد الجهاز نفسه داخل `Backup`، وليس مجلد `Backup` العام.

---

## 5. فحص النسخة باستخدام imessage-exporter

`imessage-exporter` أداة مفتوحة المصدر تقرأ قواعد رسائل Apple الحديثة، وتتعامل مع حالات لا يلتقطها SQL البسيط مثل النص الموجود في `attributedBody`.

- [الإصدارات الرسمية](https://github.com/ReagentX/imessage-exporter/releases)
- [شرح العمل على Windows](https://github.com/ReagentX/imessage-exporter/discussions/258)

استخدم أحدث إصدار. الإصدارات الحديثة تدعم النسخ المشفرة وتطلب كلمة المرور تفاعلياً. لا تمرر كلمة المرور في سطر الأوامر ولا تحفظها في ملف المشروع.

### 5.1 التشخيص قبل التصدير

من PowerShell داخل مجلد الأداة:

```powershell
$n9IphoneBackup = "C:\Users\win\Apple\MobileSync\Backup\ضع-معرف-النسخة-هنا"
.\imessage-exporter.exe -a iOS -p $n9IphoneBackup -d
```

يجب تمرير **جذر نسخة الجهاز**. لا تمرر ملف `sms.db` وحده، لأن ذلك قد يفصل قاعدة البيانات عن المرفقات وملف Manifest.

سجل مخرجات التشخيص، خصوصاً:

```text
Total messages
Messages not associated with a chat
Messages belonging to more than one chat
Total attachments
Data referenced in table
Data present on disk
Missing files
```

### 5.2 نسخة عرض للمراجعة البشرية

بعد نجاح التشخيص، يمكن إنشاء نسخة HTML أو TXT للمراجعة فقط:

```powershell
$n9IphoneBackup = "C:\Users\win\Apple\MobileSync\Backup\ضع-معرف-النسخة-هنا"
$n9ExportPath = "D:\N9-iPhone-Messages-Review"

.\imessage-exporter.exe `
  -a iOS `
  -p $n9IphoneBackup `
  -f html `
  -c clone `
  -o $n9ExportPath
```

`clone` ينسخ المرفقات كما هي من دون تحويل. التحويل إلى JPEG/MP4 ليس ضرورياً لمرحلة التدقيق، وقد يتطلب ImageMagick أو FFmpeg.

إذا اختلفت الخيارات في إصدار مستقبلي، نفذ:

```powershell
.\imessage-exporter.exe --help
```

ولا تخمّن أسماء الخيارات.

---

## 6. طريقة Mac: أفضل مصدر لمحتوى Messages in iCloud

إذا كانت الرسائل القديمة موجودة في iCloud ولا تصل كاملة إلى نسخة iPhone على Windows، فوجود Mac متزامن غالباً أفضل مسار متاح للمستخدم.

### 6.1 المزامنة

1. سجل الدخول إلى Mac بنفس Apple Account.
2. افتح Messages.
3. افتح Messages → Settings → iMessage.
4. فعّل **Enable Messages in iCloud**.
5. اترك الجهاز متصلاً بالإنترنت والطاقة.
6. لا تعتمد على الوقت فقط؛ افتح محادثات معروفة وتحقق من ظهور أقدم رسالة متوقعة.

Apple تنبه أن ظهور كل الرسائل أول مرة قد يستغرق وقتاً: [إعداد Messages in iCloud على Mac](https://support.apple.com/guide/icloud/set-up-messages-mm0de0d4528d/icloud)

### 6.2 حفظ المصدر

بعد اكتمال المزامنة والتحقق:

1. أغلق تطبيق Messages تماماً.
2. احتفظ بنسخة من مجلد:

```text
~/Library/Messages/
```

أهم الملفات:

```text
chat.db
chat.db-wal
chat.db-shm
Attachments/
```

وجود `-wal` و`-shm` مهم إذا كانت هناك معاملات لم تندمج بعد داخل قاعدة SQLite. الأفضل نسخ مجلد Messages كاملاً بعد إغلاق التطبيق.

قد يحتاج Terminal أو أداة الاستخراج إلى **Full Disk Access** لقراءة هذا المجلد.

### 6.3 التشخيص على Mac

يمكن تشغيل `imessage-exporter` على قاعدة Mac الافتراضية أو تحديد `chat.db` صراحة، ثم مقارنة عدد الرسائل وتواريخها مع نسخة iPhone.

---

## 7. كيف نثبت أن الاستخراج كامل؟

نجاح الأداة لا يعني اكتمال البيانات. أنشئ تقرير تحقق قبل XML.

### 7.1 أرقام إجمالية

سجل لكل مصدر:

| البند | المطلوب |
|---|---|
| اسم المصدر | iPhone الحالي، نسخة قديمة، Mac، إلخ |
| وقت إنشاء المصدر | التاريخ والوقت والمنطقة الزمنية |
| عدد الرسائل | كل السجلات الصالحة |
| عدد محادثات SMS | محادثات الخدمة SMS |
| أقدم رسالة | التاريخ الأصلي |
| أحدث رسالة | التاريخ الأصلي |
| رسائل بلا نص | مع فصل رسائل المرفقات عن الأعطال |
| مرفقات مسجلة | العدد داخل القاعدة |
| مرفقات موجودة | العدد الموجود فعلياً على القرص |
| مرفقات مفقودة | العدد والمسارات |
| رسائل بلا محادثة | العدد ومعرفاتها |

### 7.2 محادثات مرجعية

اختر 5–10 محادثات تعرفها، ومنها أقدم محادثة وأكبر محادثة ورسائل شركات. لكل محادثة قارن:

- الاسم/الرقم.
- أول تاريخ على الهاتف أو Mac.
- أول تاريخ في المستخرج.
- آخر تاريخ.
- عدد تقريبي أو دقيق للرسائل.
- رسالة قديمة معروفة بنص أو رقم مميز.
- رسالة حديثة معروفة.

مثال قرار:

```text
المحادثة على الجهاز تبدأ في 2021-03-10
المستخرج يبدأ في 2024-08-02
النتيجة: المصدر ناقص ولا يُعتمد وحده
```

### 7.3 مؤشرات النقص

- أقدم تاريخ أحدث مما تعرفه.
- محادثات كاملة غير موجودة.
- عدد كبير من الرسائل بلا نص وبلا مرفق.
- جميع المرفقات Missing.
- نسخة الجهاز صغيرة بشكل غير منطقي مقارنة بحجم Messages في تخزين iPhone.
- التصدير يحتوي iMessage فقط ولا يحتوي SMS، أو العكس.
- وجود فلتر تاريخ أو بحث أثناء التصدير.

---

## 8. استخراج SMS فقط

قاعدة Apple تجمع عدة خدمات، منها:

- `SMS`
- `iMessage`
- `RCS` في الأنظمة المدعومة
- رسائل نظام وتفاعلات ومرفقات

لا تفترض قيم الخدمة قبل فحص المصدر. في قاعدة SQLite المستخرجة يمكن البدء بتقرير قراءة فقط:

```sql
SELECT service, COUNT(*) AS message_count
FROM message
GROUP BY service
ORDER BY message_count DESC;
```

إذا كان هدف N9 رسائل الشركات الخضراء فقط، استورد سجلات `SMS` بعد التحقق من القيم الفعلية. لا تحوّل iMessage أو RCS إلى SMS من دون وضع علامة على المصدر والخدمة الأصلية.

---

## 9. التحويل إلى XML متوافق مع N9

### 9.1 الشكل الأساسي

N9 يقرأ عقد `<sms>` ويحتاج الحقول التالية:

```xml
<?xml version="1.0" encoding="UTF-8"?>
<smses count="1">
  <sms
    address="+966500000000"
    contact_name="اسم المرسل"
    date="1788220800000"
    date_sent="1788220740000"
    type="1"
    read="1"
    status="0"
    body="نص الرسالة"
    n9_source="iphone"
    n9_service="SMS"
    n9_guid="معرف Apple الأصلي"
    n9_chat_guid="معرف المحادثة الأصلي"
  />
</smses>
```

### 9.2 خريطة الحقول

| مصدر Apple | XML / N9 | القاعدة |
|---|---|---|
| `message.guid` | `n9_guid` | المعرف الأقوى لمنع التكرار |
| `chat.guid` | `n9_chat_guid` | يحافظ على هوية المحادثة |
| `handle.id` أو أعضاء المحادثة | `address` | الرقم أو البريد أو معرف المحادثة |
| اسم جهة الاتصال | `contact_name` | اسم العرض، وليس مفتاح الدمج الوحيد |
| `message.service` | `n9_service` | SMS/iMessage/RCS كما ورد |
| `message.is_from_me = 0` | `type="1"` | واردة |
| `message.is_from_me = 1` | `type="2"` | صادرة |
| تاريخ Apple الأصلي | `date` | Unix milliseconds من دون تقريب |
| تاريخ الإرسال إن توفر | `date_sent` | لا يُخترع عند الغياب |
| النص أو `attributedBody` | `body` | النص الكامل بعد فك بنية Apple |
| حالة القراءة | `read` | تحفظ إن كانت موثوقة |

### 9.3 التواريخ

بعض إصدارات Apple تخزن الوقت بوحدات مختلفة منذ 1 يناير 2001. لا تطبق معادلة ثابتة من دون اكتشاف حجم القيمة وإصدار القاعدة.

للقيم الحديثة التي تكون nanoseconds منذ Apple epoch، تكون الفكرة:

```text
Unix milliseconds = floor(Apple nanoseconds / 1,000,000) + 978,307,200,000
```

لكن يجب اختبار التحويل على رسائل ذات وقت معروف. لا تستخدم `Date.now()` لتعويض قيمة مفقودة، ولا تجعل كل الرسائل في نفس الوقت.

### 9.4 النصوص الحديثة

في إصدارات iOS الحديثة قد يكون `message.text` فارغاً بينما النص موجود داخل `attributedBody`. لذلك:

- لا تعتمد على استعلام `SELECT text FROM message` فقط.
- استخدم parser حديثاً يفك attributedBody.
- سجّل الرسائل التي فشل فكها بدلاً من إسقاطها بصمت.

### 9.5 المرفقات

صيغة N9 الحالية تركز على SMS النصية. لا تضع مسار مرفق داخل نص الرسالة وتدعي أنه النص الأصلي. احفظ حزمة منفصلة:

```text
iphone-messages.xml
attachments/
attachments-manifest.json
extraction-report.json
hashes.sha256
```

يحتوي manifest على:

- `message_guid`
- اسم الملف الأصلي
- النوع MIME
- الحجم
- SHA-256
- حالة موجود/مفقود
- المسار النسبي داخل الحزمة

---

## 10. دمج رسائل iPhone مع XML موجود

لا تعدل ملف XML الأصلي مباشرة. أنشئ ملف ناتج جديداً.

### 10.1 مفتاح منع التكرار

الأولوية:

1. `message.guid` من Apple.
2. عند غياب GUID: بصمة SHA-256 من:

```text
service + chat_guid + address + type + exact_timestamp + exact_body
```

لا تعتمد على النص وحده أو الرقم وحده.

### 10.2 خطوات الدمج

1. قراءة XML الحالي دون تغيير قيمه.
2. قراءة كل مصادر Apple.
3. تطبيع صيغة الأرقام للبحث فقط، مع حفظ القيمة الأصلية.
4. إزالة التكرار بين مصادر Apple بالـGUID.
5. إزالة التكرار مع XML بالبصمة الحذرة.
6. إبقاء كل تعارض غير محسوم للمراجعة اليدوية.
7. ترتيب الرسائل بالتاريخ الأصلي.
8. كتابة XML جديد UTF-8.
9. إنشاء تقرير قبل/بعد.
10. مقارنة عينة من الرسائل بالنص والتاريخ والاتجاه.

### 10.3 تقرير الدمج المطلوب

```text
رسائل XML الأصلية
رسائل iPhone المقروءة
رسائل SMS
رسائل iMessage
رسائل RCS
رسائل جديدة أضيفت
رسائل مكررة بالـGUID
رسائل مكررة بالبصمة
تعارضات تحتاج مراجعة
رسائل بلا نص
مرفقات موجودة
مرفقات مفقودة
أقدم وأحدث تاريخ قبل الدمج
أقدم وأحدث تاريخ بعد الدمج
```

---

## 11. حالات خاصة لا تُحوّل بصمت

- المحادثات الجماعية: `address` واحد غير كافٍ؛ احفظ `chat_guid` والأعضاء.
- Tapbacks والتفاعلات: ليست SMS مستقلة عادية.
- الرسائل المعدلة: احفظ الإصدار الأصلي والحالي إن أمكن.
- Unsent messages: لا تعرضها كرسالة عادية من دون وسم.
- رسائل المرفقات بلا نص: لا تعتبرها رسالة فارغة تالفة تلقائياً.
- رسائل Business Chat: قد تحتوي payload منظم وليس نصاً فقط.
- الرسائل المحذوفة أو المستعادة: ضع وسم المصدر والحالة.
- اختلاف المنطقة الزمنية: خزّن اللحظة الأصلية، واعرضها بتوقيت الرياض أو الجهاز من دون تغيير القيمة.

---

## 12. الأمان وسلسلة الحفظ

نسخة iPhone قد تحتوي كلمات مرور محفوظة، صحة، سجل اتصال، صوراً وبيانات شديدة الحساسية.

- لا ترفع مجلد Apple Backup إلى GitHub أو Vercel.
- لا تضع كلمة تشفير النسخة داخل `.env` متعقب أو مستند.
- نفذ فك التشفير والتحويل محلياً.
- احفظ الأصل للقراءة فقط، واعمل على نسخة.
- أنشئ SHA-256 للمصادر والناتج.
- سجل وقت الاستخراج، اسم الأداة وإصدارها.
- لا تحذف النسخ القديمة بعد نجاح أول تصدير؛ تحقق أولاً من الاكتمال.
- لا تستعد نسخة قديمة على هاتفك الأساسي بغرض التجربة قبل إنشاء نسخة حديثة مستقلة.

مثال بصمة ملف:

```powershell
Get-FileHash -Algorithm SHA256 -LiteralPath "D:\N9-iPhone-Source\sms.db"
```

---

## 13. شجرة القرار السريعة

```text
هل الرسائل كاملة على iPhone؟
├─ نعم → نسخة Apple جديدة → تشخيص قاعدة sms.db → تحويل XML
└─ لا
   ├─ موجودة على Mac متزامن؟
   │  ├─ نعم → حفظ ~/Library/Messages → تشخيص chat.db → دمج
   │  └─ لا
   ├─ موجودة في نسخة Apple قديمة؟
   │  ├─ نعم → استخراج النسخة القديمة → دمج بالـGUID
   │  └─ لا
   ├─ موجودة في Recently Deleted؟
   │  ├─ نعم → استردادها أولاً ثم إنشاء مصدر جديد
   │  └─ لا
   └─ لا يوجد مصدر يحتويها → لا يمكن ضمان استعادتها
```

---

## 14. تعريف النجاح

لا يعتبر الاستخراج ناجحاً إلا إذا:

- عُرف عدد الرسائل الكلي في كل مصدر.
- عُرف أقدم وأحدث تاريخ.
- تمت مقارنة محادثات مرجعية يدوياً.
- لم تُقص الرسائل بسبب فلتر أو حد واجهة.
- فُك attributedBody أو سُجل فشله صراحة.
- عُرف عدد المرفقات المفقودة.
- حُفظ GUID والخدمة والاتجاه والتاريخ الأصلي.
- لم تتغير XML timestamps.
- لم تختلط SMS وiMessage من دون وسم.
- لم تُحذف تعارضات الدمج بصمت.
- حُفظ الأصل والناتج مع SHA-256 وتقرير استخراج.

---

## 15. ما يحتاجه محول N9 المستقبلي

الأداة الأنسب للمشروع هي محول محلي مستقل باسم مقترح:

```text
N9 iPhone Backup Converter
```

وظائفه:

1. اختيار جذر نسخة Apple أو مجلد Messages من Mac.
2. دعم النسخ المشفرة محلياً.
3. اكتشاف `Manifest.db` و`sms.db` أو `chat.db` تلقائياً.
4. فحص قاعدة البيانات قبل الاستيراد.
5. عرض عدد SMS/iMessage/RCS والمحادثات والتواريخ.
6. عرض المرفقات المفقودة والرسائل غير القابلة للفك.
7. اختيار «SMS فقط» أو جميع الخدمات.
8. توليد XML متوافق مع N9.
9. دمج اختياري مع XML موجود باستخدام GUID والبصمة.
10. إنشاء تقرير JSON وCSV وبصمات SHA-256.

حتى يتم بناء هذا المحول، لا تستخدم تصدير iMazing باعتباره دليلاً على أن الأرشيف كامل. يمكن استخدامه للمراجعة البصرية فقط، بينما تكون قاعدة Apple الأصلية وتقرير الاكتمال هما مصدر القرار.

